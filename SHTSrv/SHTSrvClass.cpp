/*----- PROTECTED REGION ID(SHTSrvClass.cpp) ENABLED START -----*/
// kate: replace-tabs off; indent-width 4; indent-mode cstyle; remove-trailing-spaces all; tab-indents on; tab-width 4
//=============================================================================
//
// file :        SHTSrvClass.cpp
//
// description : C++ source for the SHTSrvClass.
//               A singleton class derived from DeviceClass.
//               It implements the command and attribute list
//               and all properties and methods required
//               by the SHTSrv once per process.
//
// project :     Sensiron SHT server
//
// This file is part of Tango device class.
//
// Tango is free software: you can redistribute it and/or modify
// it under the terms of the GNU General Public License as published by
// the Free Software Foundation, either version 3 of the License, or
// (at your option) any later version.
//
// Tango is distributed in the hope that it will be useful,
// but WITHOUT ANY WARRANTY; without even the implied warranty of
// MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
// GNU General Public License for more details.
//
// You should have received a copy of the GNU General Public License
// along with Tango.  If not, see <http://www.gnu.org/licenses/>.
//
// $Author:  $
//
// $Revision:  $
// $Date:  $
//
// $HeadURL:  $
//
//=============================================================================
//                This file is generated by POGO
//        (Program Obviously used to Generate tango Object)
//=============================================================================


#include <SHTSrvClass.h>

/*----- PROTECTED REGION END -----*/	//	SHTSrvClass.cpp

//-------------------------------------------------------------------
/**
 *	Create SHTSrvClass singleton and
 *	return it in a C function for Python usage
 */
//-------------------------------------------------------------------
extern "C" {
#ifdef _TG_WINDOWS_

__declspec(dllexport)

#endif

	Tango::DeviceClass *_create_SHTSrv_class(const char *name) {
		return SHTSrv_ns::SHTSrvClass::init(name);
	}
}

namespace SHTSrv_ns
{
//===================================================================
//	Initialize pointer for singleton pattern
//===================================================================
SHTSrvClass *SHTSrvClass::_instance = NULL;

//--------------------------------------------------------
/**
 * method : 		SHTSrvClass::SHTSrvClass(string &s)
 * description : 	constructor for the SHTSrvClass
 *
 * @param s	The class name
 */
//--------------------------------------------------------
SHTSrvClass::SHTSrvClass(string &s):Tango::DeviceClass(s)
{
	cout2 << "Entering SHTSrvClass constructor" << endl;
	set_default_property();
	write_class_property();

	/*----- PROTECTED REGION ID(SHTSrvClass::constructor) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::constructor

	cout2 << "Leaving SHTSrvClass constructor" << endl;
}

//--------------------------------------------------------
/**
 * method : 		SHTSrvClass::~SHTSrvClass()
 * description : 	destructor for the SHTSrvClass
 */
//--------------------------------------------------------
SHTSrvClass::~SHTSrvClass()
{
	/*----- PROTECTED REGION ID(SHTSrvClass::destructor) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::destructor

	_instance = NULL;
}


//--------------------------------------------------------
/**
 * method : 		SHTSrvClass::init
 * description : 	Create the object if not already done.
 *                  Otherwise, just return a pointer to the object
 *
 * @param	name	The class name
 */
//--------------------------------------------------------
SHTSrvClass *SHTSrvClass::init(const char *name)
{
	if (_instance == NULL)
	{
		try
		{
			string s(name);
			_instance = new SHTSrvClass(s);
		}
		catch (bad_alloc &)
		{
			throw;
		}
	}
	return _instance;
}

//--------------------------------------------------------
/**
 * method : 		SHTSrvClass::instance
 * description : 	Check if object already created,
 *                  and return a pointer to the object
 */
//--------------------------------------------------------
SHTSrvClass *SHTSrvClass::instance()
{
	if (_instance == NULL)
	{
		cerr << "Class is not initialised !!" << endl;
		exit(-1);
	}
	return _instance;
}



//===================================================================
//	Command execution method calls
//===================================================================
//--------------------------------------------------------
/**
 * method : 		getTemperatureClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *getTemperatureClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "getTemperatureClass::execute(): arrived" << endl;
	const Tango::DevVarLongArray *argin;
	extract(in_any, argin);
	return insert((static_cast<SHTSrv *>(device))->get_temperature(argin));
}

//--------------------------------------------------------
/**
 * method : 		getHumidityClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *getHumidityClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "getHumidityClass::execute(): arrived" << endl;
	const Tango::DevVarLongArray *argin;
	extract(in_any, argin);
	return insert((static_cast<SHTSrv *>(device))->get_humidity(argin));
}

//--------------------------------------------------------
/**
 * method : 		getDataClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *getDataClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "getDataClass::execute(): arrived" << endl;
	const Tango::DevVarLongArray *argin;
	extract(in_any, argin);
	return insert((static_cast<SHTSrv *>(device))->get_data(argin));
}


//===================================================================
//	Properties management
//===================================================================
//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::get_class_property()
 *	Description : Get the class property for specified name.
 */
//--------------------------------------------------------
Tango::DbDatum SHTSrvClass::get_class_property(string &prop_name)
{
	for (unsigned int i=0 ; i<cl_prop.size() ; i++)
		if (cl_prop[i].name == prop_name)
			return cl_prop[i];
	//	if not found, returns  an empty DbDatum
	return Tango::DbDatum(prop_name);
}

//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::get_default_device_property()
 *	Description : Return the default value for device property.
 */
//--------------------------------------------------------
Tango::DbDatum SHTSrvClass::get_default_device_property(string &prop_name)
{
	for (unsigned int i=0 ; i<dev_def_prop.size() ; i++)
		if (dev_def_prop[i].name == prop_name)
			return dev_def_prop[i];
	//	if not found, return  an empty DbDatum
	return Tango::DbDatum(prop_name);
}

//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::get_default_class_property()
 *	Description : Return the default value for class property.
 */
//--------------------------------------------------------
Tango::DbDatum SHTSrvClass::get_default_class_property(string &prop_name)
{
	for (unsigned int i=0 ; i<cl_def_prop.size() ; i++)
		if (cl_def_prop[i].name == prop_name)
			return cl_def_prop[i];
	//	if not found, return  an empty DbDatum
	return Tango::DbDatum(prop_name);
}


//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::set_default_property()
 *	Description : Set default property (class and device) for wizard.
 *                For each property, add to wizard property name and description.
 *                If default value has been set, add it to wizard property and
 *                store it in a DbDatum.
 */
//--------------------------------------------------------
void SHTSrvClass::set_default_property()
{
	string	prop_name;
	string	prop_desc;
	string	prop_def;
	vector<string>	vect_data;

	//	Set Default Class Properties

	//	Set Default device Properties
	prop_name = "SamplingPeriod";
	prop_desc = "Sampling period in ms of the sensor (should be kept above 1s to minimize heating effects in the sensor)";
	prop_def  = "3000";
	vect_data.clear();
	vect_data.push_back("3000");
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);
	prop_name = "ClockPin";
	prop_desc = "Sensor clock GPIO pin number";
	prop_def  = "";
	vect_data.clear();
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);
	prop_name = "DataPin";
	prop_desc = "Sensor data GPIO pin number";
	prop_def  = "";
	vect_data.clear();
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);
	prop_name = "RingBufferLength";
	prop_desc = "Length of ring buffer";
	prop_def  = "1000";
	vect_data.clear();
	vect_data.push_back("1000");
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);
}

//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::write_class_property()
 *	Description : Set class description fields as property in database
 */
//--------------------------------------------------------
void SHTSrvClass::write_class_property()
{
	//	First time, check if database used
	if (Tango::Util::_UseDb == false)
		return;

	Tango::DbData	data;
	string	classname = get_name();
	string	header;
	string::size_type	start, end;

	//	Put title
	Tango::DbDatum	title("ProjectTitle");
	string	str_title("Sensiron SHT server");
	title << str_title;
	data.push_back(title);

	//	Put Description
	Tango::DbDatum	description("Description");
	vector<string>	str_desc;
	str_desc.push_back("Device server to acquire Sensiron SHT temperature and humidity sensors.");
	description << str_desc;
	data.push_back(description);

	//  Put inheritance
	Tango::DbDatum	inher_datum("InheritedFrom");
	vector<string> inheritance;
	inheritance.push_back("TANGO_BASE_CLASS");
	inher_datum << inheritance;
	data.push_back(inher_datum);

	//	Call database and and values
	get_db_class()->put_property(data);
}

//===================================================================
//	Factory methods
//===================================================================

//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::device_factory()
 *	Description : Create the device object(s)
 *                and store them in the device list
 */
//--------------------------------------------------------
void SHTSrvClass::device_factory(const Tango::DevVarStringArray *devlist_ptr)
{
	/*----- PROTECTED REGION ID(SHTSrvClass::device_factory_before) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::device_factory_before

	//	Create devices and add it into the device list
	for (unsigned long i=0 ; i<devlist_ptr->length() ; i++)
	{
		cout4 << "Device name : " << (*devlist_ptr)[i].in() << endl;
		device_list.push_back(new SHTSrv(this, (*devlist_ptr)[i]));
	}

	//	Manage dynamic attributes if any
	erase_dynamic_attributes(devlist_ptr, get_class_attr()->get_attr_list());

	//	Export devices to the outside world
	for (unsigned long i=1 ; i<=devlist_ptr->length() ; i++)
	{
		//	Add dynamic attributes if any
		SHTSrv *dev = static_cast<SHTSrv *>(device_list[device_list.size()-i]);
		dev->add_dynamic_attributes();

		//	Check before if database used.
		if ((Tango::Util::_UseDb == true) && (Tango::Util::_FileDb == false))
			export_device(dev);
		else
			export_device(dev, dev->get_name().c_str());
	}

	/*----- PROTECTED REGION ID(SHTSrvClass::device_factory_after) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::device_factory_after
}
//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::attribute_factory()
 *	Description : Create the attribute object(s)
 *                and store them in the attribute list
 */
//--------------------------------------------------------
void SHTSrvClass::attribute_factory(vector<Tango::Attr *> &att_list)
{
	/*----- PROTECTED REGION ID(SHTSrvClass::attribute_factory_before) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::attribute_factory_before
	//	Attribute : Temperature
	TemperatureAttrib	*temperature = new TemperatureAttrib();
	Tango::UserDefaultAttrProp	temperature_prop;
	temperature_prop.set_description("Temperature in degrees celsius");
	temperature_prop.set_label("Temperature");
	temperature_prop.set_unit("°C");
	temperature_prop.set_standard_unit("°C");
	//	display_unit	not set for Temperature
	temperature_prop.set_format("%.1f");
	//	max_value	not set for Temperature
	//	min_value	not set for Temperature
	//	max_alarm	not set for Temperature
	//	min_alarm	not set for Temperature
	//	max_warning	not set for Temperature
	//	min_warning	not set for Temperature
	//	delta_t	not set for Temperature
	//	delta_val	not set for Temperature

	temperature->set_default_properties(temperature_prop);
	//	Not Polled
	temperature->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(temperature);

	//	Attribute : Humidity
	HumidityAttrib	*humidity = new HumidityAttrib();
	Tango::UserDefaultAttrProp	humidity_prop;
	humidity_prop.set_description("Relative humidity");
	humidity_prop.set_label("Relative humidity");
	humidity_prop.set_unit("%");
	humidity_prop.set_standard_unit("%");
	//	display_unit	not set for Humidity
	//	format	not set for Humidity
	//	max_value	not set for Humidity
	//	min_value	not set for Humidity
	//	max_alarm	not set for Humidity
	//	min_alarm	not set for Humidity
	//	max_warning	not set for Humidity
	//	min_warning	not set for Humidity
	//	delta_t	not set for Humidity
	//	delta_val	not set for Humidity

	humidity->set_default_properties(humidity_prop);
	//	Not Polled
	humidity->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(humidity);

	//	Attribute : DewPoint
	DewPointAttrib	*dewpoint = new DewPointAttrib();
	Tango::UserDefaultAttrProp	dewpoint_prop;
	dewpoint_prop.set_description("Dew point in degrees celsius");
	dewpoint_prop.set_label("Dew point");
	dewpoint_prop.set_unit("°C");
	dewpoint_prop.set_standard_unit("°C");
	//	display_unit	not set for DewPoint
	//	format	not set for DewPoint
	//	max_value	not set for DewPoint
	//	min_value	not set for DewPoint
	//	max_alarm	not set for DewPoint
	//	min_alarm	not set for DewPoint
	//	max_warning	not set for DewPoint
	//	min_warning	not set for DewPoint
	//	delta_t	not set for DewPoint
	//	delta_val	not set for DewPoint

	dewpoint->set_default_properties(dewpoint_prop);
	//	Not Polled
	dewpoint->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(dewpoint);

	//	Attribute : Timestamp
	TimestampAttrib	*timestamp = new TimestampAttrib();
	Tango::UserDefaultAttrProp	timestamp_prop;
	timestamp_prop.set_description("Timestamp in ms of last sensor acquisition");
	timestamp_prop.set_label("Timestamp");
	timestamp_prop.set_unit("ms");
	timestamp_prop.set_standard_unit("ms");
	//	display_unit	not set for Timestamp
	//	format	not set for Timestamp
	//	max_value	not set for Timestamp
	//	min_value	not set for Timestamp
	//	max_alarm	not set for Timestamp
	//	min_alarm	not set for Timestamp
	//	max_warning	not set for Timestamp
	//	min_warning	not set for Timestamp
	//	delta_t	not set for Timestamp
	//	delta_val	not set for Timestamp

	timestamp->set_default_properties(timestamp_prop);
	//	Not Polled
	timestamp->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(timestamp);

	//	Attribute : BufferSize
	BufferSizeAttrib	*buffersize = new BufferSizeAttrib();
	Tango::UserDefaultAttrProp	buffersize_prop;
	//	description	not set for BufferSize
	//	label	not set for BufferSize
	//	unit	not set for BufferSize
	//	standard_unit	not set for BufferSize
	//	display_unit	not set for BufferSize
	//	format	not set for BufferSize
	//	max_value	not set for BufferSize
	//	min_value	not set for BufferSize
	//	max_alarm	not set for BufferSize
	//	min_alarm	not set for BufferSize
	//	max_warning	not set for BufferSize
	//	min_warning	not set for BufferSize
	//	delta_t	not set for BufferSize
	//	delta_val	not set for BufferSize

	buffersize->set_default_properties(buffersize_prop);
	//	Not Polled
	buffersize->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(buffersize);

	//	Attribute : FormatTypes
	FormatTypesAttrib	*formattypes = new FormatTypesAttrib();
	Tango::UserDefaultAttrProp	formattypes_prop;
	//	description	not set for FormatTypes
	//	label	not set for FormatTypes
	//	unit	not set for FormatTypes
	//	standard_unit	not set for FormatTypes
	//	display_unit	not set for FormatTypes
	//	format	not set for FormatTypes
	//	max_value	not set for FormatTypes
	//	min_value	not set for FormatTypes
	//	max_alarm	not set for FormatTypes
	//	min_alarm	not set for FormatTypes
	//	max_warning	not set for FormatTypes
	//	min_warning	not set for FormatTypes
	//	delta_t	not set for FormatTypes
	//	delta_val	not set for FormatTypes

	formattypes->set_default_properties(formattypes_prop);
	//	Not Polled
	formattypes->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(formattypes);

	//	Attribute : FormatSizes
	FormatSizesAttrib	*formatsizes = new FormatSizesAttrib();
	Tango::UserDefaultAttrProp	formatsizes_prop;
	//	description	not set for FormatSizes
	//	label	not set for FormatSizes
	//	unit	not set for FormatSizes
	//	standard_unit	not set for FormatSizes
	//	display_unit	not set for FormatSizes
	//	format	not set for FormatSizes
	//	max_value	not set for FormatSizes
	//	min_value	not set for FormatSizes
	//	max_alarm	not set for FormatSizes
	//	min_alarm	not set for FormatSizes
	//	max_warning	not set for FormatSizes
	//	min_warning	not set for FormatSizes
	//	delta_t	not set for FormatSizes
	//	delta_val	not set for FormatSizes

	formatsizes->set_default_properties(formatsizes_prop);
	//	Not Polled
	formatsizes->set_disp_level(Tango::OPERATOR);
	//	Not Memorized
	att_list.push_back(formatsizes);


	//	Create a list of static attributes
	create_static_attribute_list(get_class_attr()->get_attr_list());
	/*----- PROTECTED REGION ID(SHTSrvClass::attribute_factory_after) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::attribute_factory_after
}
//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::pipe_factory()
 *	Description : Create the pipe object(s)
 *                and store them in the pipe list
 */
//--------------------------------------------------------
void SHTSrvClass::pipe_factory()
{
	/*----- PROTECTED REGION ID(SHTSrvClass::pipe_factory_before) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::pipe_factory_before
	/*----- PROTECTED REGION ID(SHTSrvClass::pipe_factory_after) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::pipe_factory_after
}
//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::command_factory()
 *	Description : Create the command object(s)
 *                and store them in the command list
 */
//--------------------------------------------------------
void SHTSrvClass::command_factory()
{
	/*----- PROTECTED REGION ID(SHTSrvClass::command_factory_before) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::command_factory_before


	//	Command getTemperature
	getTemperatureClass	*pgetTemperatureCmd =
		new getTemperatureClass("getTemperature",
			Tango::DEVVAR_LONGARRAY, Tango::DEVVAR_FLOATARRAY,
			"getTemperature(timestamp, n)",
			"Array of temperature values",
			Tango::OPERATOR);
	command_list.push_back(pgetTemperatureCmd);

	//	Command getHumidity
	getHumidityClass	*pgetHumidityCmd =
		new getHumidityClass("getHumidity",
			Tango::DEVVAR_LONGARRAY, Tango::DEVVAR_FLOATARRAY,
			"getHumidity(timestamp, n)",
			"Array of humidity values",
			Tango::OPERATOR);
	command_list.push_back(pgetHumidityCmd);

	//	Command getData
	getDataClass	*pgetDataCmd =
		new getDataClass("getData",
			Tango::DEVVAR_LONGARRAY, Tango::DEVVAR_CHARARRAY,
			"getTemperature(timestamp, n)",
			"Encoded output with triplets (uint32, float, float) representing respectively (timestamp, temperature, humidity)",
			Tango::OPERATOR);
	command_list.push_back(pgetDataCmd);

	/*----- PROTECTED REGION ID(SHTSrvClass::command_factory_after) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::command_factory_after
}

//===================================================================
//	Dynamic attributes related methods
//===================================================================

//--------------------------------------------------------
/**
 * method : 		SHTSrvClass::create_static_attribute_list
 * description : 	Create the a list of static attributes
 *
 * @param	att_list	the ceated attribute list
 */
//--------------------------------------------------------
void SHTSrvClass::create_static_attribute_list(vector<Tango::Attr *> &att_list)
{
	for (unsigned long i=0 ; i<att_list.size() ; i++)
	{
		string att_name(att_list[i]->get_name());
		transform(att_name.begin(), att_name.end(), att_name.begin(), ::tolower);
		defaultAttList.push_back(att_name);
	}

	cout2 << defaultAttList.size() << " attributes in default list" << endl;

	/*----- PROTECTED REGION ID(SHTSrvClass::create_static_att_list) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::create_static_att_list
}


//--------------------------------------------------------
/**
 * method : 		SHTSrvClass::erase_dynamic_attributes
 * description : 	delete the dynamic attributes if any.
 *
 * @param	devlist_ptr	the device list pointer
 * @param	list of all attributes
 */
//--------------------------------------------------------
void SHTSrvClass::erase_dynamic_attributes(const Tango::DevVarStringArray *devlist_ptr, vector<Tango::Attr *> &att_list)
{
	Tango::Util *tg = Tango::Util::instance();

	for (unsigned long i=0 ; i<devlist_ptr->length() ; i++)
	{
		Tango::DeviceImpl *dev_impl = tg->get_device_by_name(((string)(*devlist_ptr)[i]).c_str());
		SHTSrv *dev = static_cast<SHTSrv *> (dev_impl);

		vector<Tango::Attribute *> &dev_att_list = dev->get_device_attr()->get_attribute_list();
		vector<Tango::Attribute *>::iterator ite_att;
		for (ite_att=dev_att_list.begin() ; ite_att != dev_att_list.end() ; ++ite_att)
		{
			string att_name((*ite_att)->get_name_lower());
			if ((att_name == "state") || (att_name == "status"))
				continue;
			vector<string>::iterator ite_str = find(defaultAttList.begin(), defaultAttList.end(), att_name);
			if (ite_str == defaultAttList.end())
			{
				cout2 << att_name << " is a UNWANTED dynamic attribute for device " << (*devlist_ptr)[i] << endl;
				Tango::Attribute &att = dev->get_device_attr()->get_attr_by_name(att_name.c_str());
				dev->remove_attribute(att_list[att.get_attr_idx()], true, false);
				--ite_att;
			}
		}
	}
	/*----- PROTECTED REGION ID(SHTSrvClass::erase_dynamic_attributes) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::erase_dynamic_attributes
}

//--------------------------------------------------------
/**
 *	Method      : SHTSrvClass::get_attr_by_name()
 *	Description : returns Tango::Attr * object found by name
 */
//--------------------------------------------------------
Tango::Attr *SHTSrvClass::get_attr_object_by_name(vector<Tango::Attr *> &att_list, string attname)
{
	vector<Tango::Attr *>::iterator it;
	for (it=att_list.begin() ; it<att_list.end() ; ++it)
		if ((*it)->get_name()==attname)
			return (*it);
	//	Attr does not exist
	return NULL;
}


/*----- PROTECTED REGION ID(SHTSrvClass::Additional Methods) ENABLED START -----*/

/*----- PROTECTED REGION END -----*/	//	SHTSrvClass::Additional Methods
} //	namespace
